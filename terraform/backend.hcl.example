# Example only. Your workflow writes this dynamically in each job.
bucket         = "<YOUR_TF_STATE_BUCKET>"
key            = "terraform.tfstate"
region         = "<YOUR_AWS_REGION>"
dynamodb_table = "tf-locks"
encrypt        = true
